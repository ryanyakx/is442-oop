export default class User {
    constructor(username, email, password, contact) {
      this.username = username;
      this.email = email;
      this.password = password;
      this.contact = contact;
    }
  }
  